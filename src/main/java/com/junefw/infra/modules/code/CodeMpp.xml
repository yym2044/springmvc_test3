<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.junefw.infra.modules.code.CodeMpp"> <!-- 지금 파일 지정 -->

	<resultMap id="resultMapObj"
		type="com.junefw.infra.modules.code.Code"></resultMap> <!-- 결과 객체 저장 -->

	<select id="selectList" resultMap="resultMapObj">
		
		<!-- SELECT 
			a.ifcgSeq
			, a.ifcgName
			, a.ifcgDelNy
		FROM 
			cpcpcp.infrCodeGroup a
		WHERE 1=1
			AND ifcgDelNy = 0 -->
			
			select
				a.ifcgSeq
				,a.ifcgName
				,b.ifcdSeq
				,b.ifcdName
			from cpcpcp.infrcodegroup a
				left join cpcpcp.infrcode b on b.ifcgSeq = a.ifcgSeq
			where 1=1
				and a.ifcgSeq = 1
	</select>

	<insert id="insert">
		insert into infrMember (
			ifmmId
			,ifmmName
			,ifmmDelNy
		) values (
			#{ifmmId}
			,#{ifmmName}
			,0
		)
	</insert>
</mapper>